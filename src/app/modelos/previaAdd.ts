export class PreviaAdd {  
    public unidadCurricular: string;
    public previa: string;
    public tipo: string;
  
    constructor(unidadCurricular: string, previa: string, tipo: string) {
      this.unidadCurricular = unidadCurricular;
      this.previa = previa;
      this.tipo = tipo;
    }
}